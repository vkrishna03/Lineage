[project]
name = "fastapi-lineage-example"
version = "0.1.0"
description = "FastAPI service with Lineage tracking integration"
requires-python = ">=3.11"
dependencies = [
    "lineage-sdk",
    "fastapi>=0.110.0",
    "uvicorn[standard]>=0.27.0",
    "pydantic>=2.0.0",
]

[tool.uv.sources]
lineage-sdk = { path = "../../sdks/python" }
